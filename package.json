{
    "devDependencies": {
        "husky": "^8.0.1",
        "lint-staged": "^13.0.1",
        "prettier": "^2.6.2"
    },
    "lint-staged": {
        "*": "prettier --write --ignore-unknown"
    },
    "scripts": {
        "prepare": "husky install"
    }
}
