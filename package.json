{
    "devDependencies": {
        "husky": "^8.0.2",
        "lint-staged": "^13.0.3",
        "prettier": "^2.7.1"
    },
    "lint-staged": {
        "*": "prettier --write --ignore-unknown"
    },
    "scripts": {
        "prepare": "husky install"
    }
}
